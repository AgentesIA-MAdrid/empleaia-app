import { MisTurnos } from "@/components/turnos/mis-turnos";

/**
 * El horario propio de la coordinadora (ticket 73), separado del cuadrante de
 * su equipo, que vive en `/manager/turnos`.
 */
export default function MisTurnosManagerPage() {
  return <MisTurnos />;
}
